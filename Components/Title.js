function Title(props) {
    return (
        <section className="container">
            <div className="title">
                <h2>{props.title}</h2>
            </div>
        </section>
    )
}

export default Title